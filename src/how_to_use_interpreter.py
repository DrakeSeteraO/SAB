USE = """

=================================How To====================================

To bring up this page run the command:

"python src/SAB.py 123.sab 234.sab"

=========================Terminal SAB Interpreter==========================

There are multiple ways to use the SAB Compiler.

The first way is to give the interpreter instructions through the terminal.
To initialize the Terminal SAB Interpreter run the command:

"python src/SAB.py"

After running the command the terminal should now show a ">>" symbol. 
If you see the symbol then everything is all set! 
You can start typing instructions, and then press enter to execute them.

To exit run the input combination:

ctr + C + Enter

If ran correctly you should see the message ">> Exiting ..."

================================SAB Compiler===============================

The other option is to compile a file or multiple files using the SAB 
Compiler.

To run the SAB Compiler run the Terminal SAB Interpreter, but with the
name of the file you want to run passed in as an argument.

Example:

"python src/SAB.py example.sab"

To run the compiler with multiple files, just list the additional files.

"python src/SAB.py example.sab example2.sab"

===========================================================================
"""


HELP = "The SAB Compiler is a compiler designed to compile files or instructions into machine code a Subtract And Branch (SAB) emulator can run."
